# Use the official Node.js image as the base image
FROM node:14

# Set the working directory inside the container
WORKDIR /mcarbonportal
COPY . .

RUN npm install --location=global npm
RUN cd mcarbonPortal
# Build mcarbon portal frontend
RUN npm install && \
    npm run build && \
